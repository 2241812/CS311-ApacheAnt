# === Project Directories ===
src.dir=src
test.dir=test
build.dir=build
classes.dir=${build.dir}/classes
test.classes.dir=${build.dir}/test-classes
test.reports.dir=${build.dir}/test-reports
dist.dir=dist
doc.dir=doc

# === Project Metadata ===
project.name=TeamProject
project.version=1.0
team.name=Team 2
# Updated to point to the new GUI class
main.class=com.team2.MainApp

# === JAR Files ===
jar.classes.file=${project.name}.jar
jar.doc.file=${project.name}-docs.jar
file.to.compile= com/team2/solutions/arrays/ArraySolutions.java